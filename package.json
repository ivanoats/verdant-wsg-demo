{
  "name": "verdant-site",
  "version": "1.0.0",
  "private": true,
  "description": "Verdant design system, demoed as a lightweight static site starter with defaults aligned to selected Web Sustainability Guidelines. Styled with PandaCSS.",
  "type": "module",
  "scripts": {
    "predev": "panda codegen",
    "dev": "node scripts/dev.mjs",
    "prebuild": "panda codegen",
    "build": "node scripts/build.mjs && panda cssgen -m --lightningcss -o dist/styles.css && node scripts/stats.mjs && node scripts/fingerprint.mjs"
  },
  "license": "ISC",
  "devDependencies": {
    "@pandacss/dev": "^1.12.1"
  }
}
